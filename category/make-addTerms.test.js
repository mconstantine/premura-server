const makeAddTerms = require('./make-addTerms')

describe('addTerms', () => {
  it.skip('Is not done', () => {})
  // Should check that the category exists
  // Should work with a category with no terms
  // Should not save extra information
  // Should not allow terms with identical names
  // Should create an id for each term
  // Should override an eventually provided term ID
})
