const makeRemoveTerms = require('./make-removeTerms')

describe('removeTerms', () => {
  it.skip('Is not done', () => {})
  // Should check that the category exists
  // Should check that the terms exist
  // Should work
})
